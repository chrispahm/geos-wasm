[project]
name = "geos-wasm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "geopandas>=1.0.1",
    "pyarrow>=18.1.0",
    "pyogrio>=0.10.0",
]
